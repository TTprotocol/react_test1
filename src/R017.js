import React, {} from 'react';

const R017 = (props) => {
    let props_value = props.props_val;
    props_value += ' from App.js';

    return (
        <div>{props_value}</div>
    )
}

export default R017;