import React from "react";

const R024 = (props) => {
    var stateStr = props.reactString;
    var stateNum = 100;

    return (
        <div style={{ padding: "0px" }}>
            {stateStr} {stateNum}
        </div>
    );
};

export default R024;
