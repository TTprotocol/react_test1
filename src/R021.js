import React, {} from 'react';
// import datatype from 'prop-types';

const R021 = (props) => {
    let {
        ReactS,
        ReactN
    } = props

    return (
        <div style={{padding: "0px"}}>
            {ReactS}{ReactN}
        </div>
    )
}

// PropRequiered.propTypes = {
//     ReactS: datatype.isRequired,
// }

export default R021;